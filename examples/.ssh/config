Host *
    # This would keep connections active in the background
    #
    # ControlPath ~/.ssh/controlmasters/%r@%h
    # ControlMaster auto
    # ControlPersist 8h

    # Needed if you want to edit remote files with local sublime
    #
    # RemoteForward 52698 localhost:52698

    KeepAlive yes
    ServerAliveInterval 30

    AddKeysToAgent yes
    UpdateHostKeys yes
    IdentityFile ~/.ssh/id_ed25519
